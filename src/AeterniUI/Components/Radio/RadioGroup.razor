@typeparam TValue
@inherits AeterniComponent

<fieldset @attributes="BuildAttributes()" role="radiogroup" aria-label="@AriaLabel" aria-invalid="@(EffectiveInvalid ? "true" : null)" aria-required="@(EffectiveRequired ? "true" : null)">
    <CascadingValue Value="Context" IsFixed="false">
        @ChildContent
    </CascadingValue>
</fieldset>
