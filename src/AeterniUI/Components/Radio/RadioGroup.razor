@typeparam TValue
@inherits AeterniComponent

<fieldset @attributes="BuildAttributes()" role="radiogroup" aria-label="@AriaLabel" aria-invalid="@(Invalid ? "true" : null)" aria-required="@(Required ? "true" : null)">
    <CascadingValue Value="Context" IsFixed="false">
        @ChildContent
    </CascadingValue>
</fieldset>
