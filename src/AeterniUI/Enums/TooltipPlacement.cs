namespace AeterniUI.Enums;

/// <summary>
/// Preferred side of the trigger the tooltip is rendered on. The tooltip flips
/// to the opposite side and shifts along the cross axis when the preferred side
/// would leave the viewport.
/// </summary>
public enum TooltipPlacement
{
    Top,
    Bottom,
    Start,
    End
}
